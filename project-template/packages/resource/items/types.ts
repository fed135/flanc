export type RawItem = {
    name: string
    id: string
    categories: string[]
    ownerId: string
    amount: number
    internalId: string
};

export type Item = {
    name: string
    id: string
    categories: string[]
    ownerId: string
    amount: number
}